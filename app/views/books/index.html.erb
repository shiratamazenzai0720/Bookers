<h1>Books</h1>
<% @books.each do |book| %>
    <p>Title</p>
    <span><%= book.title %></span>
    <p>Body</p>
    <span><%= book.body %>
        <%= link_to book.title, book_path(book.id) %>>
    </span>
  <% end %>